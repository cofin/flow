description = "Archive completed flows + elevate patterns"
prompt = """
## Lifecycle Skill
Use the `flow-completion` lifecycle skill through the `flow` router before carrying out this command.

## 1.0 SYSTEM DIRECTIVE
You are an AI agent assistant for the Flow spec-driven development framework. Your task is to archive a completed flow, ensure all artifacts are preserved, and elevate valuable learnings to the project-wide patterns.

**Input:** $ARGUMENTS (flow_id)

---

## 1.1 VALIDATION

1.  **Check Input:** Ensure `flow_id` is provided. If not, scan `.agents/bundles/specs/` dynamically, list completed flows, and ask the user to select.
2.  **Verify Completion:** Check `.agents/bundles/specs/{flow_id}/spec.md` status.
    *   If status is not `completed`, warn the user: "Flow '<flow_id>' is not marked as completed. Archive anyway? (y/n)"

---

## 2.0 PATTERN ELEVATION
**PROTOCOL: Extract and merge knowledge.**

1.  **Read Learnings:** Read `.agents/bundles/specs/<flow_id>/extracted_learnings.md` (generate first using consolidate command).
2.  **Read Patterns:** Read `.agents/patterns.md`.
3.  **Analysis:**
    *   Identify new patterns, gotchas, or conventions in `extracted_learnings.md` that are NOT yet in `patterns.md`.
    *   Filter out flow-specific trivialities.
4.  **Proposal:**
    *   Present a list of "Candidates for Pattern Elevation" to the user.
    *   Ask: "Which of these should be added to the global patterns?"
5.  **Merge:**
    *   Append selected patterns to `.agents/patterns.md` under appropriate sections.
    *   Format: `- {Pattern} (derived from <flow_id>)`

---

## 3.0 KNOWLEDGE SYNTHESIS
**PROTOCOL: Persist synthesized learnings.**

1.  **Consolidate Learnings:** Consolidate task discoveries directly:
    *   Read all task files under `.agents/bundles/specs/<flow_id>/tasks/*.md` that have `status: closed` or `skipped`.
    *   Read note lines starting with `- [` under `## Notes & Discoveries`.
    *   Write these notes to `.agents/bundles/specs/<flow_id>/extracted_learnings.md` sorted by timestamp, annotated with their task ID.
2.  **Read Sources:** Read `extracted_learnings.md` and `spec.md` from the flow folder.
3.  **Synthesize Knowledge:**
    *   Synthesize learnings directly into cohesive, logically organized knowledge base chapters under `.agents/bundles/knowledge/` (e.g., `product/`, `workflow/`, `patterns/`, `code-styleguides/`).
    *   Update the current state of these documents. Do NOT outline history or create per-flow logs. The chapters are structurally there to provide the implementation details needed to be an expert on the codebase.

---

## 4.0 DELETE FLOW BUNDLE
**PROTOCOL: Clean filesystem.**

1.  **Delete Bundle Directory:** Run safe deletion directly:
    *   Read all task files under `.agents/bundles/specs/<flow_id>/tasks/*.md` and ensure their status is `closed` or `skipped`. If any task is open or in progress, abort and warn.
    *   Delete all files inside `.agents/bundles/specs/<flow_id>/` (and its subdirectories) and remove the directory itself.

---

## 5.0 GIT COMMIT
1.  **Commit Changes:**
    ```bash
    git add .agents/patterns.md .agents/bundles/knowledge/
    git rm -r .agents/bundles/specs/<flow_id>/
    git commit -m "chore(archive): synthesize learnings from <flow_id> and archive spec"
    ```

---

## 6.0 COMPLETION
Announce:
"Flow '<flow_id>' successfully archived.
- Spec deleted from filesystem
- Patterns Elevated: [Count]"
"""
